<Project>
  <PropertyGroup>
    <!-- Shared flat folder to collect project output DLLs -->
    <MicroLabSharedDir Condition="'$(MicroLabSharedDir)'==''">C:\MicroLabEngine</MicroLabSharedDir>
  </PropertyGroup>
</Project>